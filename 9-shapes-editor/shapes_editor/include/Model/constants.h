#pragma once

#include "Common/Point.h"
#include "Common/Color.h"

namespace model::constants
{

const common::Point TopLeftPoint = { 0, 0 };
const common::Point BottomRightPoint = { 70, 70 };
const common::Color FillColor = {0, 30, 200, 255 };
const common::Color BorderColor = { 0, 220, 70, 255 };

} // namespace model::constants
